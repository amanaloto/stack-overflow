$('#new_answer').one('ajax:success', function(){
  var $ans_form = $(this).closest(".js-answer-form");
  $ans_form.prev().html("<%= escape_javascript(render @question.answers) %>");
  $ans_form.html("");
  $(".js-submit-answer").show();
});