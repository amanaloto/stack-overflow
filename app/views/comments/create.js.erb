$(".js-new-comment[data-answer-id=<%= @answer.id %>]").one('ajax:success', function(){
  var $comments = $(this).parents("#js-answer-row").find(".list-group");
  $comments.html("<%= escape_javascript(render @answer.comments) %>");
  $(this).parents(".js-comment-form").html("");
});