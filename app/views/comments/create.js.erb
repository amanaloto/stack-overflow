$("#new_comment").one('ajax:success', function(){
  var $comments = $(this).parents(".table:first").find(".table-condensed");
  $comments.html("<%= escape_javascript(render @answer.comments) %>");
  $(this).closest("tr").html("");
});