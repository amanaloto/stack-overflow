//expand comment section
$('#expand_comments').one('ajax:success', function(){
  var $comments_list = $(this).parents('.row').find('.list-group');
  $comments_list.html('<%= escape_javascript( render @answer.comments) %>');
});