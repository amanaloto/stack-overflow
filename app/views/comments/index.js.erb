$(".expand_comments[data-answer-id=<%= @answer.id %>]")
.one('ajax:success', function(){
  var $comments_list = $(this).parents('.list-group:first');
  $comments_list.html('<%= escape_javascript( render @answer.comments) %>');
});