$(".reply").off();
$(".reply").on('ajax:success', function(){
  var $commentForm = $(this).parents('#answer_row').find('.comment-form:first');
  $commentForm.html("<%= escape_javascript(render 'comments/form') %>");
});