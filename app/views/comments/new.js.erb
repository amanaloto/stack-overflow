$(".js-reply[data-answer-id=<%= @answer.id %>]").one('ajax:success', function(){
  var $commentForm = $(this).parents('#js-answer-row').find('.js-comment-form:first');
  $commentForm.html("<%= escape_javascript(render 'comments/form') %>");
});